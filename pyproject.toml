[project]
name = "logistic-regression-filter"
version = "1.0.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "cupy>=14.0.1",
    "numpy>=2.4.3",
]

[tool.uv.workspace]
members = [
    "backend",
]
